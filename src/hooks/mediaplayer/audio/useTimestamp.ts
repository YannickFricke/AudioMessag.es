describe('useTimestamp', () => {
    // audioElement.currentTime;
    it.todo('should be able to get the current timestamp position');

    it.todo('should be able to set a new timestamp position');
});
